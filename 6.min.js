
(jQuery), function (t) {
    t((function () {
        t(document).on("keyup", (function (n) {
            "Escape" == n.key && e.parent().hasClass("active") && e.each((function () {
                t(this).parent().hasClass("active") && t(this).parent().removeClass("active")
            })), "tab" !== n.key && "TAB" !== n.key || n.preventDefault()
        }));
        var e = t(".js-open-submenu");
        e.on("click", (function (e) {
            e.preventDefault();
            var n = t(this);
            n.next().find(".sub-shadow").removeClass("active"), t(this).parent().hasClass("active") ? t(this).parent().removeClass("active") : (t(".js-navigation li.active").removeClass("active"), t(this).parent().addClass("active"), setTimeout((function () {
                n.next().find(".sub-shadow").addClass("active")
            }), 300))
        })), e.add(e.parent().next().find("a")).focus((function () {
            e.parent().removeClass("active")
        })), t(".js-open-meniu").on("click", (function (e) {
            e.preventDefault(), t(this).toggleClass("active"), t(this).hasClass("active") ? (bodyScrollLock.disableBodyScroll(t(".js-mobile-menu")[0]), t(".js-header").addClass("ss-mobile-menu-opened")) : (bodyScrollLock.clearAllBodyScrollLocks(), t(".js-header").removeClass("ss-mobile-menu-opened")), t(".js-mobile-menu").toggleClass("active")
        })), t(".sub-shadow").on("click", (function (e) {
            t(".js-navigation li.active").removeClass("active")
        }));
        const n = t(".js-header-ls");
        n.on("click", (function () {
            n.find("select")[0].showPicker()
        }));
        const a = t(".alert-bar"), o = e => {
            const n = e.outerHeight();
            t("body").css("padding-top", 64 + n + "px")
        };
        a.length && o(a), t(window).on("resize", (function () {
            a.length && o(a)
        }))
    }))
}


(jQuery), function (t) {
    t((function () {
        let e = t(".js-infinite-slider");
        if (e.length) {
            let a = [],
                o = {item: 0, loop: !0, pager: !1, enableTouch: !1, enableDrag: !1, slideMargin: 0, cssEasing: "ease"};

            function n() {
                let n;
                n = checkWidth(767) ? 3 : checkWidth(1300) ? 9 : 11, o.item = n, e.each((function () {
                    this.lightSlider || (slider = t(this).lightSlider(o), slider.play(), a.push(slider))
                }))
            }

            n(), t(window).resize((function () {
                t(a).each((function () {
                    this.lightSlider && this.destroy()
                })), n()
            }))
        }
    }))
}


